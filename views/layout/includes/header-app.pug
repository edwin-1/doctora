header.bg-slate-600.p-4
    .container.mx-auto.flex.justify-between.items-center
        a(href="/") 
            h1.text-2xl.text-white.font-extrabold.text-center Doctora
                span.font-normal Dinorath

        nav(class="my-5 text-sm md:flex md:items-center md:gap-3 font-bold text-white hidden")
            a(href="/auth/registro") Crear Cuenta
            a(href="/auth/login") Iniciar Sesion 

div(class="bg-slate-700 py-5 hidden lg:block")
    div(class="container mx-auto flex justify-between")
        nav(class="flex gap-4 items-center")
            a(href="/categorias/1" class="text-sm font-bold uppercase text-white") Atención médica integral
            a(href="/categorias/2" class="text-sm font-bold uppercase text-white") Aplicación de solución
            a(href="/categorias/3" class="text-sm font-bold uppercase text-white") Atendido por médicos certificados

        form(action="/buscador" method="POST" class="flex gap-3 items-center")
            input(type="hidden" name="_csrf" value= csrfToken)
            label(for="termino" class="text-sm uppercase font-bold text-white") Busqueda
            input(type="text" id="termino" name="termino" placeholder="Buscar Consultas" class="p-2 rounded-lg shadow text-sm")

            input(type="submit" class="bg-slate-800 hover:bg-slate-400 rounded-lg text-white uppercase font-bold p-2 cursor-pointer text-sm" value="Buscar") 